# Node基础

Node为服务的JavaScript提供了一个时间驱动的、异步的平台。他们都是事件驱动（用事件轮询）和非阻塞I/O处理（用异步I/O)

Node中也有可写数据流，可以往里面写数据块。可读和可写数据流可以连接起来形成管道，这样只有数据准备好就可以处理，不用等着读取往整个资源在把它写出去。

聊天程序需要具备的三个基本功能：

1. 给用户的Web浏览器提供静态文件
2. 在服务器端处理与聊天相关的消息
3. 在用户的Web浏览器中处理与聊天相关的信息

访问内存要比访问文件系统快得多，所有Node程序通常会把常用的数据缓存放到内存里



